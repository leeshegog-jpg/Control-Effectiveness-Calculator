// R0 scaffold placeholder — root route tree. Mounts each module's
// src/modules/<name>/routes.tsx once routing library is decided
// (TO_BE_CONFIRMED — 13-application-foundation-scaffold.md §2).
export {}
