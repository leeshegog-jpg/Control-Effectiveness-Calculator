// R0 scaffold placeholder — composition root (providers + router mount).
// Module route trees (src/modules/<name>/routes.tsx) mount here once routing
// is decided (React Router, TO_BE_CONFIRMED — 13-application-foundation-scaffold.md §2).
function App() {
  return null
}

export default App
