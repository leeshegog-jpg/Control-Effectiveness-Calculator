// R0 scaffold placeholder -- TypeScript build of ontology concept resolution.
// Exists so no module re-implements "resolve this string against the live
// concept list" independently (the V1 failure mode -- architecture §1.4
// finding 1). No resolution logic implemented yet.
export {}
