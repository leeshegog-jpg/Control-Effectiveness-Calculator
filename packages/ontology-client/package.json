{
  "name": "@sms/ontology-client",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript build of ontology concept lookup/validation. Python build lives in py/ (same package, two language targets from one conceptual spec) -- see docs/implementation-blueprint/13-application-foundation-scaffold.md §4.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@sms/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
